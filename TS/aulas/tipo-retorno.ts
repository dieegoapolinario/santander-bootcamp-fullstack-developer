function somar(number1: number, number2: number): number {
  return number1 + number2;
}

console.log(somar(1, 5))

function printValue(number1: number, number2: number): void {
  console.log(number1 + number2)
}